import Particles from "react-particles"
import { participlesConfig } from "./config-particles"

export const ParticiplesBackgroud = () => {
    return (
       <Particles params ={participlesConfig}>

       </Particles> 
    )
}